var client = require('../dev/client')()

module.exports = client
//   // if (req.isAuthenticated()) {
//   //   res.send('<a href="/logout">Sign out</a>')
//   // } else {
//   //   res.send('<a href="/login">Sign in with ORCID</a>')
//   // }
// }