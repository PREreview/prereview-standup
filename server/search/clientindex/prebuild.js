// We have an instant search on the client side that uses a prebuilt index.
// The index is regenerated regularly on the server during idle time so that
// it always contains the most popular, most recently added and most recently
// reviewed preprints.
module.exports = prebuild

function prebuild () {
	
}